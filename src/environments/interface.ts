export interface Environment {
  authDomain: string;
  databaseURL: string;
  projectId: string;
  storageBucket: string;
  messagingSenderId: string;
  appId: string;
  apiKey: string;
  production: boolean;
}
